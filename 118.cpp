#include <iostream>
#include <bits/stdc++.h>

using namespace std;


class Solution {
public:
    vector<vector<int>> generate(int numRows) {

    }
};

int main(){
    // 定义好函数中的测试数据
    // vector<int> inp = {2, 3, 4};
    int numRow = 5;
    Solution solu;
    solu.generate(numRow);
    return 0;
}